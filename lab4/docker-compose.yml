version: '3.8'

services:
  zookeeper:
    image: confluentinc/cp-zookeeper:7.0.1
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    networks:
      - mynet

  kafka:
    image: confluentinc/cp-kafka:7.0.1
    depends_on:
      - zookeeper
    ports:
      - "9092:9092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
    networks:
      - mynet

  topic-creator:
    image: confluentinc/cp-kafka:7.0.1
    depends_on:
      - kafka
    command: >
      bash -c "sleep 10 && kafka-topics --create --topic tasks --partitions 3 --replication-factor 1 --if-not-exists --bootstrap-server kafka:9092"
    networks:
      - mynet

  manager:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      - topic-creator
    environment:
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
    networks:
      - mynet

  worker:
    build:
      context: .
      dockerfile: Dockerfile.worker
    depends_on:
      - topic-creator
    environment:
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
    deploy:
      replicas: 3
    networks:
      - mynet

networks:
  mynet:
    driver: bridge
